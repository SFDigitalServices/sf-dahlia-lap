- if user_signed_in?
  li#sign-out= link_to 'Sign out', users_sign_out_path, method: 'delete'
  / alternative to needing rails-ujs, if we can get it styled correctly:
  / li= button_to 'Sign out', users_sign_out_path, method: 'delete', class: 'button button-link'
- else
  li= link_to 'Partner Sign in', omniauth_authorize_path(:user, salesforce_provider), method: :post, class: 'sign-in-btn'
  li= link_to 'MOHCD Admin Sign in', omniauth_authorize_path(:user, salesforce_provider, { admin: true }), method: :post
